---
topics: [quality-gate, test-baseline, regression]
doc_kind: bug-report
created: 2026-08-25
---

# Fork main full-gate baseline restoration

## Bug diagnosis capsule

| Field | Evidence-backed diagnosis |
|---|---|
| **1. Symptom** | Clean `fork/main` at `8bb6f72f9de47fc77bbe9c11b3f793d59ea8f33f` fails `pnpm check` on nine Biome findings and then documentation, skill-reference, and environment-governance checks; fails gate Step 4 in `packages/bootcamp-literature-reader`; fails the Web suite in seven tests; fails the Shared suite because a public test reads an excluded private dossier; lets inherited trusted-remote authority invalidate an API security test; and fails three public API tests after a governance/source sync. A host without the required `tmux` executable also cannot run the real terminal integration suites. The Web package's five real-browser files additionally import Playwright through the excluded `packages/ppt-forge` workspace, so a public checkout cannot load them; after assigning the dependency to Web, the browser run exposes a replaced-node focus race and a 13-case suite whose 120-second aggregate timeout expires even though the individual assertions pass. Once those pass, Web's final two commands reference an emoji-guard self-test and implementation that do not exist in any exported Git ref. The final feature-truth pass also finds F153 marked `in-progress` while every declared AC is checked, even though its Phase H still contains a real exporter-test backlog item that was never represented in Acceptance Criteria. |
| **2. Evidence** | Biome reports exactly nine non-F306 files. After those are formatted, `check-feature-truth` reports F306's plural `## User Journeys` heading and F153's absent journey; the next check reports F076 lacks either its own capability tip or an explicit exemption, even though its user-facing projection is superseded and covered by F306. The review-completion routing check then requires the logical `refs/chatgpt-review-user-journey.md` token while the skill-reference integrity gate rejects that depth-dependent coordinate and requires `../.cat-cafe-shared-refs/...`; the referenced file and stable alias both exist. The environment registry check reports `CODEX_APP_SERVER_SOCKET`, `CHATGPT_APP_CODEX_BIN`, and `KIMI_CODE_HOME`; call-site inspection shows all three are host/provider-owned path overrides rather than Hub-editable runtime configuration, matching the allowlist's intended boundary. The bootcamp package has no `tsconfig.json`, yet the recursive shell finds the root `tsc` and exits with TypeScript help status 1. The full Web run reports 7 failures out of 6,569 tests: three F232 fixture assertions, three API timeout/retry assertions, and one adaptive pass-ball assertion. A source-suite run later exposes one Shared failure: `load-dossier-profiles.test.ts` reads `docs/team/cat-dossier.md`, but that private file is intentionally absent in this public repository, so the documented ENOENT community behavior returns an empty profile map. The same run inherits `CAT_CAFE_TRUST_REMOTE_SINGLE_USER=1` from the launching runtime, so `workspace-navigation-auth.test.js` treats the injected remote address as authorized and observes 200 instead of 401. On the validation host, all 38 tmux tests are cancelled at suite setup until the missing executable is installed; with real tmux present, the unchanged integration assertions pass 38/38. Forcing source `full` mode also reaches `signal-fetcher-launchd-script.test.js`, but the public exclusion registry classifies it as `private_fixture` and the auto-mode harness tests require public checkouts to run `test:public`; the absent private launchd wrappers are therefore not a public-gate defect. The auto-selected public suite then exposes three further clean-main failures: the generated development SOP contains 30 rules (17 `manual_only`) while its integration test still expects the pre-sync 24/11 counts, and two F275 invocation fixtures return thread-shaped objects without the required persisted `id`, causing the new Review-workspace identity guard to dereference `undefined`. The same two files fail 3/202 on clean main and pass 202/202 after synchronizing those exact contracts. A clean-main browser-file run fails before discovery with `ERR_MODULE_NOT_FOUND` for `packages/ppt-forge/node_modules/playwright/index.mjs`; the removed workspace and its historical lock both identify Playwright `1.61.1` as the browser baseline. With Web owning that exact dependency, the F299 evidence test catches focus loss when a render replaces the first focused node; the repaired implementation and two focused unit regressions pass 7/7, and the real browser evidence passes. The F294 browser parent then cancels later subtests at 120 seconds while completed cases are green; retaining every content and screenshot assertion, bounding each child at 30 seconds, and setting a 180-second aggregate budget yields 13/13 child cases and 15/15 total tests green in 239.7 seconds for the combined F299/F294 run. The next full Web run passes all 22 browser tests and then fails `MODULE_NOT_FOUND` on `scripts/check-no-new-ui-emoji.test.mjs`; history shows the same sync commit appended both the self-test and implementation commands while exporting neither file. A new isolated-Git harness proves the intended incremental contract Red→Green: new production TSX emoji blocks, unchanged legacy and test/fixture/SVG additions pass, and an invalid base fails closed (3/3). |
| **3. Root cause** | Independent baseline drifts accumulated: mechanical formatting was not committed; active/superseded specs do not fully declare their journey and capability-tip governance; F153's still-open Phase H exporter-test backlog was omitted from its AC list, making the status and declared acceptance set contradict each other; two skill-governance gates encoded incompatible reference coordinates for the same required document; newly introduced host/provider path overrides omitted the required reasoned environment allowlist classification; one public-sync test retained an internal-repository data dependency whose source file is intentionally excluded; the TypeScript gate equates a PATH-visible `tsc` with a configured workspace; the API test-home wrapper strips other runtime authority but omitted `CAT_CAFE_TRUST_REMOTE_SINGLE_USER`; public-sync fixtures retained source-era repository/path values, pre-GET-coordination fetch doubles, and incomplete persisted thread records; the generated SOP gained six manual Review-governance rules without updating the integration test's exact contract counts; and the adaptive replay roster omitted the punctuation-bearing `co-creator` handle despite its existing red test. Separately, Web's browser runner retained a cross-workspace dependency after `ppt-forge` became a private/excluded package, the Eval focus retry treated invoking `focus()` as success even when a render replaced that node, F294's growing scenario matrix retained a suite-wide budget shorter than its bounded sequential execution, and the public sync copied package-script references without their required emoji-guard closure. The tmux cancellation is a validation-host prerequisite failure, not a product assertion failure. |
| **4. Diagnosis strategy** | Reproduce on an isolated branch from clean `fork/main`; run each failing test file alone; compare F232 expectations with `ThreadArtifactDTO.ref`, API expectations with the bounded session retry and `Response` contracts, the pass-ball test with the explicit known-handle list, SOP counts with the generated definition, and thread doubles with `IThreadStore`. Add regression tests before changing Step 4 or the test-home environment boundary. Preserve the real tmux integration suites and satisfy their executable prerequisite rather than skipping or replacing them with fakes. |
| **5. Timeout strategy** | Use the existing bounded fetch constants and fake timers; do not change production retry counts or request timeouts. Keep every F294 scenario bounded at 30 seconds and the sequential parent bounded at 180 seconds, without changing readiness, screenshot, geometry, or rejection assertions. The final full gate runs under its existing singleflight and per-test timeout controls. |
| **6. Early warning** | Reject any fix that skips a configured TypeScript project, removes `Response.clone()` coordination, weakens F232 path assertions, broadens pass-ball detection to arbitrary mid-sentence mentions, inherits runtime authorization in a test sandbox, fakes/skips tmux integration, removes a referenced guard because its exported implementation is missing, or relaxes a gate assertion. |
| **7. User-visible correction** | Replay recognizes a line-leading `@co-creator` handoff. Other changes restore truthful checks and fixtures without changing artifact routing or API retry behavior. |
| **8. Acceptance** | The new gate and test-home regressions fail before their harness corrections and pass after them; the workspace authentication suite passes 7/7 with remote spoofing still rejected; the real tmux suites pass 38/38; the SOP/F275 focused files pass 202/202; the trajectory navigation unit regressions pass 7/7; all five real-browser files pass from Web's own Playwright dependency; the full Web and Shared suites turn green; the nine files pass Biome; Feature Truth accepts both active specs; then the exact-HEAD auto-selected public pre-merge gate, GitHub CI, non-author review, and merge to `fork/main` are green. |

### Late gate finding: F153 discoverability

After the F153 truth correction turned Feature Truth green, the capability-tip contribution gate correctly identified that the changed, user-visible observability feature had no discoverability entry. F153 is not an internal-only harness: its existing Hub surface exposes health, Traces, Prompt X-Ray, and redacted metrics. The fix therefore adds a source-backed tip that teaches the existing `设置 → 运维监控 → 监控面板` journey and opens a Concierge explanation draft. It does not claim `tips_exempt`, create a new surface, or change telemetry/runtime behavior.

## Reproduction record

1. Create an external worktree from clean `fork/main` at `8bb6f72f` and install with the frozen lockfile.
2. Run `pnpm check`; Biome reports nine formatting/import-order errors.
3. From `packages/bootcamp-literature-reader`, run the same recursive gate payload: `pnpm exec bash -lc 'if command -v tsc >/dev/null 2>&1; then tsc --noEmit; fi'`. It prints TypeScript help and exits 1 because the workspace has no `tsconfig.json`.
4. Run `pnpm --filter @cat-cafe/web test`; Vitest reports 7 failed and 6,562 passed tests across 766 files, plus two unhandled rejections caused by incomplete fetch doubles.
5. Launch the API test wrapper with `CAT_CAFE_TRUST_REMOTE_SINGLE_USER=1` and run `workspace-navigation-auth.test.js`; the remote-spoof assertion receives 200 instead of 401 because the wrapper leaves the authority grant inherited.
6. On a host without `tmux`, run the three tmux integration files; their suite constructors fail before 38 tests can execute. Install the real executable and rerun the same assertions to separate the host prerequisite from repository behavior.
7. Force `CAT_CAFE_GATE_TEST_MODE=full` in the public checkout; `signal-fetcher-launchd-script.test.js` fails because its private wrappers are intentionally absent. The public-suite resolver reports that file as excluded, while auto mode selects `test:public` as required by the gate contract.
8. Run the SOP evaluator and `invoke-single-cat` tests on clean main; 199/202 pass, while the stale 24-rule expectation and two incomplete thread fixtures fail. Re-run after contract synchronization; all 202 pass.
9. Run any Web browser file on clean main; Node fails to resolve Playwright through the excluded `ppt-forge` workspace. After assigning the exact historical Playwright version to Web, run the F299 and F294 files to expose and repair the focus race and aggregate timeout independently, then run all five browser files together.
10. Run the complete Web command after the browser files pass; Node fails to find both `check-no-new-ui-emoji` files. Add the real incremental guard and its isolated-Git contract tests instead of removing the two package-script commands.
11. Run the final repository check after adding F153's missing User Journey. `check-feature-truth` reports `all-ac-status-drift`; compare the unchecked Phase H backlog in `What` with the Acceptance Criteria and add the missing, explicitly blocked AC instead of marking the whole feature done or weakening the checker.
12. Re-run the repository check. `check-capability-tips` reports F153 has neither a tip nor an exemption. Confirm the existing Hub monitoring surface, then add a source-backed F153 tip rather than hiding that user-visible capability behind `tips_exempt`.

## Scope classification

- Mechanical: nine Biome-only findings.
- Documentation contract: use the exact singular User Journey heading for F306, add the missing operator flow to F153, and represent F153's still-open Phase H exporter-test backlog as an explicitly blocked unchecked AC so `in-progress` remains truthful.
- Capability-tip contract: explicitly record that superseded F076 discovery is owned by F306's existing project-workspaces tip.
- Capability-tip contract: give F153's existing Hub monitoring journey a real source-backed discovery tip; do not exempt a user-visible surface.
- Skill/harness contract: align the required review user-journey token with the stable shared-ref alias already enforced by reference integrity.
- Environment governance: classify host/provider-owned executable, socket, and home-path overrides as explicit non-Hub exceptions; keep runtime config unchanged.
- Public fixture contract: exercise the complete Fable engagement policy through a temporary dossier instead of depending on private repository data.
- Harness: gate Step 4 needs to distinguish configured TypeScript projects from JS-only workspaces.
- Security test harness: test-home isolation must remove trusted-remote runtime authority unless a test explicitly opts in.
- Validation environment: retain real tmux integration coverage and satisfy its native executable prerequisite.
- Public/source boundary: use the gate's auto-selected complete public suite; separately run full Web and Shared suites for the affected cross-package baseline instead of importing private launchd assets.
- Synced-test contract: retain exact SOP rule-count coverage at the current 30/17 definition and provide persisted thread identity in F275 invocation doubles.
- Browser dependency contract: Web owns the exact Playwright version used by its five browser tests instead of importing it through an excluded workspace.
- Product behavior: Eval trajectory restoration verifies that the current event node actually owns focus and retries when rendering replaces it.
- Browser harness timing: preserve all 13 F294 scenario assertions under explicit 30-second child bounds and a realistic 180-second sequential-suite bound.
- Public export harness: restore the missing F056 production-UI emoji debt-freeze implementation and its fail-closed self-test.
- Product behavior: the explicit pass-ball handle roster is missing `co-creator`.
- Test-contract drift: F232 uses the runtime `ref` contract and generic repository links correctly; API GET coordination correctly requires clonable `Response` objects and retries a failed session bootstrap within the same call.

No production Redis, user data, runtime configuration, request timeout, retry budget, or F306 branch is in scope.
